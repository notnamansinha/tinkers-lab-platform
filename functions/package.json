{
  "name": "functions",
  "private": true,
  "description": "Cloud Functions for Tinkers' Lab Platform — server-side enforcement: booking conflict detection, overdue sweep, notifications, feedback rate limiting.",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.3.2"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
