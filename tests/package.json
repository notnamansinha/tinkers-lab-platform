{
  "name": "rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore & Storage security-rule tests (run inside Firebase emulators — requires Java)",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "~6.0.2",
    "vitest": "^4.0.0"
  }
}
